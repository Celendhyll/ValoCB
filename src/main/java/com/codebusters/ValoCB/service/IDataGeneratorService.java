package com.codebusters.ValoCB.service;

/**
 * Interface that handles the generation of data from the CSV files.
 */
public interface IDataGeneratorService {

    /**
     * Generate all data from the csv files (in resources).
     */
    void generateData();
}
